mutation CreateReview($input: ReviewInput!) {
    createReview(input: $input) {
        id
        createdAt
        updatedAt
        watchedOn
        text
        score
        creatorId
        referenceId
    }
}
