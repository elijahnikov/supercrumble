mutation SettingsChangePassword(
    $currentPassword: String!
    $settingsNewPassword: String!
    $username: String!
) {
    settingsChangePassword(
        currentPassword: $currentPassword
        settingsNewPassword: $settingsNewPassword
        username: $username
    ) {
        ...UserResponse
    }
}
