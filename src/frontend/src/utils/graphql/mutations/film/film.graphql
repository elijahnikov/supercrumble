query Film($movieId: Int!) {
    film(movieId: $movieId) {
        id
        movieId
        movieTitle
        overview
        posterPath
        backdropPath
        releaseDate
        watchCount
        listCount
        likeCount
        createdAt
        updatedAt
    }
}
