query Film($movieId: Int!) {
    film(movieId: $movieId) {
        movieId
        movieTitle
        overview
        posterPath
        backdropPath
        releaseDate
        watchCount
        listCount
        likeCount
        createdAt
        updatedAt
    }
}
