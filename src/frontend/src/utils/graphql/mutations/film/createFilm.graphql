mutation CreateFilm($input: FilmInput!) {
    createFilm(input: $input) {
        id
        movieId
        movieTitle
        overview
        posterPath
        backdropPath
        releaseDate
        watchCount
        listCount
        likeCount
        createdAt
        updatedAt
    }
}
